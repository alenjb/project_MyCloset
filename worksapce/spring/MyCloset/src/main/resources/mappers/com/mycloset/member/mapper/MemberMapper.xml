<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycloset.mapper.MemberMapper">

	<!-- 1. 리스트 -->
	<select id="list" resultType="com.mycloset.member.vo.MemberVO">
		select rownum rnum, no, title, writer
		from(
			select no, title, writer
			from board
			order by no desc
			)
			where rnum between 0 and 3
	</select>
</mapper>